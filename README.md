# 📈 Line Stock Portfolio Bot

บอทไลน์สำหรับจัดการพอร์ตหุ้นส่วนตัว สามารถเพิ่มหุ้นและติดตามผลกำไร/ขาดทุนได้แบบเรียลไทม์

## ✨ ความสามารถ

- เพิ่มหุ้นเข้าพอร์ต (รองรับทั้งหุ้นไทยและต่างประเทศ)
- ดูสถานะพอร์ตแบบเรียลไทม์
- แสดงกำไร/ขาดทุนทั้งในรูปแบบ THB และ USD
- ระบบแนะนำ Ticker Symbol ที่ถูกต้อง
- แสดง Logo บริษัทอัตโนมัติ

## 🔧 ความต้องการของระบบ

- Python 3.8 หรือสูงกว่า
- PostgreSQL 12 หรือสูงกว่า
- pip (Python package manager)
- LINE Developer Account
- ngrok (สำหรับการทดสอบ local)

## 📱 การตั้งค่า LINE Bot

1. สร้าง LINE Bot ใน [LINE Developers Console](https://developers.line.biz/console/):
   - สร้าง Provider
   - สร้าง Channel ประเภท Messaging API
   - ได้ Channel Secret และ Channel Access Token

2. ตั้งค่า Webhook:
   - เปิดใช้งาน Webhook ใน LINE Developers Console
   - URL รูปแบบ: `https://your-domain/webhook`
   - สำหรับทดสอบ local ใช้ ngrok: `ngrok http 5000`
   - ใส่ URL จาก ngrok ในการตั้งค่า Webhook

3. Bot Settings แนะนำ:
   - เปิด Auto-reply messages: OFF
   - เปิด Webhook: ON
   - เปิด Allow bot to join group chats: OFF
   - Greeting message: ตามต้องการ

## �🚀 การติดตั้ง

1. Clone โปรเจค:
```bash
git clone <your-repo-url>
cd LineBot
```

2. สร้างและเปิดใช้งาน Virtual Environment:

Windows:
```powershell
# สร้าง virtual environment
python -m venv venv

# เปิดใช้งาน virtual environment
.\venv\Scripts\activate

# ถ้าต้องการออกจาก virtual environment ใช้คำสั่ง
deactivate
```

Linux/Mac:
```bash
# สร้าง virtual environment
python3 -m venv venv

# เปิดใช้งาน virtual environment
source venv/bin/activate

# ถ้าต้องการออกจาก virtual environment ใช้คำสั่ง
deactivate
```

3. ติดตั้ง dependencies (ต้องทำใน virtual environment):
```bash
pip install -r requirements.txt
```

3. สร้างไฟล์ `.env` และใส่ค่า credentials:
```env
# LINE Bot credentials
LINE_CHANNEL_SECRET=your_channel_secret
LINE_CHANNEL_ACCESS_TOKEN=your_channel_access_token

# PostgreSQL Database credentials
DB_HOST=your_db_host
DB_PORT=your_db_port
DB_NAME=your_db_name
DB_USER=your_db_user
DB_PASS=your_db_password
```

5. ตั้งค่าฐานข้อมูล PostgreSQL:
```sql
-- สร้างฐานข้อมูล
CREATE DATABASE line_bot_db;

-- สร้างตาราง portfolio
CREATE TABLE portfolio (
    id SERIAL PRIMARY KEY,
    line_user_id TEXT NOT NULL,
    ticker TEXT NOT NULL,
    shares REAL NOT NULL,
    cost_price REAL NOT NULL,
    added_on TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- สร้าง index สำหรับการค้นหา
CREATE INDEX idx_line_user_id ON portfolio(line_user_id);
```

6. รัน server (ต้องอยู่ใน virtual environment):
```bash
# Windows
python app.py

# Linux/Mac
python3 app.py
```

💡 **หมายเหตุ**: 
- ทุกครั้งที่จะรันโปรแกรม ต้องเปิดใช้งาน virtual environment ก่อนเสมอ
- Bot ต้องการ Webhook URL ที่เป็น HTTPS สำหรับการใช้งานจริง
- แนะนำให้ใช้ ngrok สำหรับการทดสอบในเครื่อง local

## � การทำงานของระบบ

1. **การจัดการข้อมูล**:
   - ใช้ Yahoo Finance API ในการดึงข้อมูลหุ้นและอัตราแลกเปลี่ยน
   - เก็บข้อมูลพอร์ตใน PostgreSQL
   - รองรับหุ้นไทย, หุ้น US, ETF และ Crypto

2. **การคำนวณ**:
   - แปลงค่าเงินอัตโนมัติจาก USD เป็น THB
   - คำนวณ P/L แบบ Unrealized
   - คำนวณต้นทุนเฉลี่ยต่อหุ้น

3. **ระบบความปลอดภัย**:
   - แยกพอร์ตตาม LINE User ID
   - Timeout สำหรับ session การเพิ่มหุ้น (5 นาที)
   - Validate input ทุกขั้นตอน

## �📝 วิธีใช้งาน

### เพิ่มหุ้น
มี 2 วิธี:

1. แบบทีละขั้นตอน:
```
พิมพ์: เพิ่มหุ้น AAPL
บอทจะถามจำนวนหุ้นและราคาทีละอย่าง
```

2. แบบรวดเดียว:
```
พิมพ์: AAPL 10 35000
(Ticker จำนวนหุ้น ราคาต่อหุ้น)
```

### ดูพอร์ต
```
พิมพ์: ดูพอร์ต
```

## 🏗️ โครงสร้างโปรเจค

```
LineBot/
├── app.py              # Entry point
├── logic.py           # Business logic
├── database.py        # Database operations
├── components/        # UI Components
│   ├── error_message.py
│   ├── stock_confirmation.py
│   ├── stock_help.py
│   ├── stock_suggestion.py
│   └── portfolio_view.py
└── utils/            # Utility functions
    ├── stock_utils.py
    └── search_utils.py
```

## 📦 Components

- **error_message.py**: จัดการการแสดงข้อผิดพลาดและข้อความทั่วไป
- **stock_confirmation.py**: แสดงการยืนยันการเพิ่มหุ้น
- **stock_help.py**: แสดงวิธีการใช้งาน
- **stock_suggestion.py**: แสดงคำแนะนำ Ticker
- **portfolio_view.py**: แสดงพอร์ตโฟลิโอ

## 🛠️ Utils

- **stock_utils.py**: จัดการเกี่ยวกับหุ้น (validate, get logo)
- **search_utils.py**: ค้นหาและแนะนำ Ticker

## 📝 หมายเหตุ

- หุ้นไทยต้องเติม .BK (เช่น PTT.BK)
- หุ้น US ไม่ต้องเติมอะไร (เช่น AAPL)
- ราคาเป็นสกุลเงินบาทเท่านั้น
- ข้อมูลราคาหุ้นและอัตราแลกเปลี่ยนใช้ Yahoo Finance API
- Logo ใช้ CryptoIcons API สำหรับ crypto และ Yahoo Finance API สำหรับหุ้น
- ใช้ Clearbit API เป็น fallback สำหรับ logo ที่ไม่พบ

## ❗ Troubleshooting

1. **ปัญหาการเชื่อมต่อ Database**:
   - ตรวจสอบ credentials ใน .env
   - ตรวจสอบว่า PostgreSQL ทำงานอยู่
   - ตรวจสอบ firewall อนุญาตพอร์ต 5432

2. **ปัญหา LINE Bot ไม่ตอบกลับ**:
   - ตรวจสอบ Webhook URL ถูกต้อง
   - ตรวจสอบ Channel Secret และ Access Token
   - เช็ค Log ใน LINE Developers Console

3. **ปัญหา Yahoo Finance API**:
   - บาง Ticker อาจต้องรอ cache refresh
   - ถ้าเรียก API บ่อยเกินไปอาจโดน rate limit
   - ใช้ proxy หรือ VPN ถ้าจำเป็น

## 📋 Todo

- [ ] เพิ่มฟีเจอร์ขายหุ้น
- [ ] เพิ่มการแจ้งเตือนราคา
- [ ] เพิ่มกราฟแสดงผลการลงทุน
- [ ] รองรับการแสดงผลภาษาอังกฤษ

## 📄 License

MIT License - ดูรายละเอียดใน [LICENSE](LICENSE) ไฟล์

## 👨‍💻 Contributing

1. Fork โปรเจค
2. สร้าง branch ใหม่ (`git checkout -b feature/amazing-feature`)
3. Commit การเปลี่ยนแปลง (`git commit -m 'Add some amazing feature'`)
4. Push ไปที่ branch (`git push origin feature/amazing-feature`)
5. เปิด Pull Request

